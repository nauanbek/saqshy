# Production stack with Nginx reverse proxy
# Usage: docker-compose -f docker/docker-compose.prod.yml --project-directory . up -d
#
# Note: All paths are relative to project root (for Coolify compatibility)
#
# Prerequisites:
#   1. Copy .env.example to .env.prod and fill in production values
#   2. Place SSL certificates in docker/nginx/ssl/
#   3. Build Mini App: cd mini_app_frontend && npm run build
#   4. Run migrations: docker-compose -f docker/docker-compose.prod.yml exec bot alembic upgrade head

services:
  postgres:
    image: postgres:16-alpine
    container_name: saqshy-postgres-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - backend

  redis:
    image: redis:7-alpine
    container_name: saqshy-redis-prod
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - backend

  qdrant:
    image: qdrant/qdrant:latest
    container_name: saqshy-qdrant-prod
    volumes:
      - qdrant_data:/qdrant/storage
    environment:
      QDRANT__SERVICE__GRPC_PORT: 6334
    deploy:
      resources:
        limits:
          memory: 1G
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:6333/readyz"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: always
    networks:
      - backend

  bot:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: production
    container_name: saqshy-bot-prod
    env_file:
      - .env.prod
    environment:
      # Override connection strings for container network
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      QDRANT_URL: http://qdrant:6333
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: always
    networks:
      - backend
      - frontend

  nginx:
    image: nginx:1.25-alpine
    container_name: saqshy-nginx-prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
      - ./mini_app_frontend/dist:/usr/share/nginx/html/app:ro
    depends_on:
      bot:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: always
    networks:
      - frontend

volumes:
  postgres_data:
  redis_data:
  qdrant_data:

networks:
  backend:
    name: saqshy-backend
  frontend:
    name: saqshy-frontend
